{
  "name": "opscenter",
  "private": true,
  "version": "0.1.0",
  "description": "Self-hosted control panel for dedicated game servers (Node panel + .NET host agent)",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix web",
    "build:web": "npm run build --prefix web",
    "start": "node scripts/start.mjs",
    "dev": "node scripts/dev.mjs"
  }
}
