{
  "name": "a3panel",
  "private": true,
  "version": "0.1.0",
  "description": "A3Panel — Node control plane + React UI",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix web",
    "build:web": "npm run build --prefix web",
    "start": "node scripts/start.mjs",
    "dev": "node scripts/dev.mjs"
  }
}
